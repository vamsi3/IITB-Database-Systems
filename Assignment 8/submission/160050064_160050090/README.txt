The url can be changed in config.dart

all .dart files and pubspec.yaml can be found in dart/
all .java files can be found in java/

search in the chats screen checks if the input is a substring to either uid or name.
search in the Create Conversation page check only the prefix (i.e, as given by AutoCompleteServlet).